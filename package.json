{
  "name": "t-jardin",
  "private": true,
  "scripts": {
    "dev:api": "npm --prefix backend run start:dev",
    "dev:web": "npm --prefix frontend start",
    "seed": "npm --prefix backend run prisma:seed",
    "migrate": "npm --prefix backend exec prisma migrate dev"
  }
}
